import 'package:flutter/material.dart';

class AdvancedVersion extends StatelessWidget {
  const AdvancedVersion({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
